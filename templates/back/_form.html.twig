<div class="col-6 mx-auto mt-3">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-warning d-block mx-auto mt-3">{{ button_label|default('Ajouter') }}</button>
    {{ form_end(form) }}
</div>